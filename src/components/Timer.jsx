"use client"

// Timer component is now integrated into PlayerCard
// This file is kept for backward compatibility but the component is no longer used
const Timer = ({ time, isActive }) => {
  return null
}

export default Timer
